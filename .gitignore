# dependencies
node_modules/

# package managers (keep bun.lock for reproducible builds)
package-lock.json
yarn.lock
pnpm-lock.yaml

# runtime files
*.log
*.sqlite

# build output (dist/ is committed for deployment)
build/

# environment files
.env
.env.*

# OS junk
.DS_Store
